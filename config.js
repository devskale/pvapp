// This is the config file that stores all the parameters

const Config = {
    kW_per_panel: 0.4,
    m2_per_kW: 2 / 0.4,
    cost_per_kW: 2500,
    no_of_panels_per_kW: 1 / 0.4,
    kWh_per_year_per_kW: 1025
};

export default Config;
